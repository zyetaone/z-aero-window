{
	"name": "aero-2",
	"private": true,
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "AERO_ADMIN_UI=1 vite dev",
		"build": "vite build",
		"preview": "vite preview",
		"serve": "bun run server.ts",
		"start": "bun run build && bun run server.ts",
		"prepare": "svelte-kit sync",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json && node tools/check-repo.mjs",
		"check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
		"test": "node tools/check-repo.mjs && vitest run",
		"test:watch": "vitest",
		"lint": "prettier --check .",
		"format": "prettier --write .",
		"smoke": "node tools/smoke-routes.mjs"
	},
	"dependencies": {
		"@svelte-maplibre-gl/pmtiles": "^2.0.1",
		"maplibre-gl": "^6.6.0",
		"svelte-maplibre-gl": "^2.2.1",
		"three": "^0.185.1",
		"zod": "^4.6.5"
	},
	"devDependencies": {
		"@sveltejs/adapter-node": "next",
		"@sveltejs/kit": "3.0.0-next.25",
		"@sveltejs/vite-plugin-svelte": "^7.3.0",
		"@types/three": "^0.185.4",
		"happy-dom": "^20.11.6",
		"jsqr": "^1.4.0",
		"pngjs": "^7.0.0",
		"prettier": "^3.9.6",
		"prettier-plugin-svelte": "^4.1.1",
		"svelte": "^5.56.10",
		"svelte-check": "^4.7.6",
		"typescript": "^6.0.3",
		"vite": "^8.2.2",
		"vitest": "^4.1.11"
	},
	"imports": {
		"#lib/*": "./src/lib/*"
	}
}
